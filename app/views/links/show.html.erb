<div class = 'col-md-8 offset-2'>
  <h1><%=@link.link_name%></h1>
  <p><%=@post.description%></p>
  <p><small>Tags: <%= raw @link.tags.map(&:name_tag).map
    { |t| link_to t, tag_path(t) }.join(', ') %></small</p>
</div>
